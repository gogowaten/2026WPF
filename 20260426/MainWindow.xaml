<Window x:Class="_20260426.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260426" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top">
      <local:RootItemsControl x:Name="MyRootItems" MyData="{Binding MyData}"/>
    </ScrollViewer>
    <!--<Canvas x:Name="MyCanvas">

    </Canvas>-->

    <StackPanel Grid.Column="1">
      <GroupBox Header="MyData.CurrentItemData" DataContext="{Binding MyData.CurrentItemData}">
        <StackPanel>
          <TextBlock Text="{Binding Width, StringFormat='Width = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding X, StringFormat='X = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding InternalX, StringFormat='InternalX = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding StringFormat=' {0: 0; -0; 0}'}"/>
        </StackPanel>
      </GroupBox>
    </StackPanel>
  </Grid>
</Window>
