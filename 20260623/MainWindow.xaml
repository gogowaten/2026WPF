<Window x:Class="_20260623.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260623"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer x:Name="MyScroll"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
      <Grid x:Name="GridToZome"
            RenderOptions.BitmapScalingMode="NearestNeighbor">
        <Grid.LayoutTransform>
          <ScaleTransform ScaleX="{Binding ElementName=MyScale, Path=Value}"
                          ScaleY="{Binding ElementName=MyScale, Path=Value}"/>
        </Grid.LayoutTransform>

        <Image x:Name="MyImage"/>
        <Rectangle  IsHitTestVisible="False">
          <Rectangle.Fill>
            <DrawingBrush TileMode="Tile" Viewport="0,0,1,1" ViewportUnits="Absolute">
              <DrawingBrush.Drawing>
                <GeometryDrawing>
                  <GeometryDrawing.Pen>
                    <Pen Brush="LightGray" Thickness="0.01"/>
                  </GeometryDrawing.Pen>
                  <GeometryDrawing.Geometry>
                    <GeometryGroup>
                      <LineGeometry StartPoint="0,0" EndPoint="1,0" />
                      <LineGeometry StartPoint="0,0" EndPoint="0,1" />
                    </GeometryGroup>
                  </GeometryDrawing.Geometry>
                </GeometryDrawing>
              </DrawingBrush.Drawing>
            </DrawingBrush>
          </Rectangle.Fill>
        </Rectangle>
      </Grid>
    </ScrollViewer>

    <StackPanel Grid.Column="1">
      <Slider x:Name="MyScale"
        Minimum="10" Maximum="100"
        IsSnapToTickEnabled="True"
        TickFrequency="1"
        SmallChange="1" LargeChange="1"
        />
    </StackPanel>
  </Grid>
</Window>
