<Window x:Class="_20260330_02_ResizeAdorner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260330_02_ResizeAdorner"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas" Background="DeepSkyBlue">
      <Rectangle x:Name="TargetRect" Canvas.Left="100" Canvas.Top="100" Width="50" Height="50" Fill="DodgerBlue"/>
    </Canvas>

    <StackPanel Grid.Column="1">
      <TextBlock Text="{Binding ElementName=TargetRect, Path=Width ,StringFormat='Width = {0:0}'}"/>
      <TextBlock Text="{Binding ElementName=TargetRect, Path=Height ,StringFormat='Height = {0:0}'}"/>
    </StackPanel>
  </Grid>
</Window>