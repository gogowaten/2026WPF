<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:_20260224_DataTemplate_Behavior_CustomCtrl"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    
  >

  <Style TargetType="{x:Type local:CustomControl1}">
    <Style.Resources>

      <DataTemplate DataType="{x:Type local:Items}">
        <Canvas>
          <ItemsControl ItemsSource="{Binding Children}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <Canvas/>
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
              <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                <Setter Property="Canvas.Top" Value="{Binding Y}"/>
              </Style>
            </ItemsControl.ItemContainerStyle>
          </ItemsControl>
        </Canvas>
      </DataTemplate>

      <DataTemplate DataType="{x:Type local:TextBlockItem}">
        <Border Background="Snow" BorderBrush="LightSkyBlue" BorderThickness="2" CornerRadius="5" Padding="10,5">
          <TextBlock Text="{Binding Text}" Foreground="Red" FontWeight="Bold">
              <i:Interaction.Behaviors>
                <local:MouseDragBehavior IsEnabled="{Binding CanDrag}"/>
              </i:Interaction.Behaviors>
          </TextBlock>
        </Border>
      </DataTemplate>

      <DataTemplate DataType="{x:Type local:RectangleItem}">
        <Rectangle Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Fill}">
          <i:Interaction.Behaviors>
            <local:MouseDragBehavior IsEnabled="{Binding CanDrag}"/>
          </i:Interaction.Behaviors>
        </Rectangle>
      </DataTemplate>

    </Style.Resources>

    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:CustomControl1}">
          <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                  DataContext="{Binding RelativeSource={RelativeSource AncestorType=local:CustomControl1}, Path=MyVM}">
            <!--<Border.DataContext>
              <local:MainViewModel/>
            </Border.DataContext>-->

            <Grid>
              <ItemsControl ItemsSource="{Binding RootItems}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <Canvas/>
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                  <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                  </Style>
                </ItemsControl.ItemContainerStyle>
              </ItemsControl>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
