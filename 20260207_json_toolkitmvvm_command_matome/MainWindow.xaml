<Window x:Class="_20260207_json_toolkitmvvm_command_matome.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260207_json_toolkitmvvm_command_matome" d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <StackPanel Margin="20">
      <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
               FontSize="{Binding FontSize}"
               FontWeight="{Binding FontWeight}"
               Margin="0,0,0,10" Height="60"/>
    </StackPanel>

    <StackPanel Grid.Column="1">
      <Slider Minimum="8" Maximum="40" Value="{Binding FontSize}" Margin="0,10"/>
      <Button Content="リセット(条件：文字あり＆サイズ>10"
              Command="{Binding ResetCommand}"
              Padding="10,5"/>
    </StackPanel>
  </Grid>
</Window>
