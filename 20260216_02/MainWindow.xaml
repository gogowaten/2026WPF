<Window x:Class="_20260216_02.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260216_02"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <local:NodeContainer ViewModel="{Binding Root}"/>

    <StackPanel Grid.Column="1">
      <Button x:Name="Add" Content="add" Command="{Binding AddNodeCommand}"/>
      <Button x:Name="Add2" Content="add" Command="{Binding AddNodeCommand}" CommandParameter="100,200"/>
    </StackPanel>
  </Grid>
</Window>
