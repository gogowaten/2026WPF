<Window x:Class="_20260510.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260510" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="567" Width="854">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top">
      <local:RootItemsControl x:Name="MyRootItems"
                              UseLayoutRounding="True"
                              MyData="{Binding MyData}"/>

    </ScrollViewer>

    <Canvas x:Name="MyCanvas">

    </Canvas>

    <StackPanel Grid.Column="1">
      <Button x:Name="check" Content="check" Click="check_Click"/>
      <Button Content="グループ化" Command="{Binding MyData.AddGroupFromSelectedItemsCommand}"/>
      <Button Content="グループ解除" Command="{Binding MyData.UnGroupCurrentCommand}"/>
      <Button x:Name="background" Content="ContextMenu" Click="ContextMenu_Click"/>
      <Button x:Name="vertexhandle" Content="ZUP" Click="Zup"/>
      <Button Content="ZupCommand" Command="{Binding MyData.ZUpSelectedItemsCommand}"/>
      <Button x:Name="RemoveEndPoint" Content="RemoveEndPoint" Click="RemoveEndPoint_Click"/>
      <Button x:Name="InsertStartPoint" Content="InsertStartPoint" Click="InsertStartPoint_Click"/>

      <GroupBox Header="CurrentData" DataContext="{Binding MyData.CurrentItemData}">
        <StackPanel>
          <TextBlock Text="{Binding StringFormat=' {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Width, StringFormat='Width = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Points, StringFormat='Points = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding X, StringFormat='X = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Y, StringFormat='Y = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Z, StringFormat='Z = {0: 0; -0; 0}'}"/>

          <TextBlock Text="{Binding StrokeThickness, StringFormat='StrokeThickness = {0: 0; -0; 0}'}"/>
          <Slider Value="{Binding StrokeThickness}" Minimum="1" Maximum="50"
                  SmallChange="10" LargeChange="10"/>
          <Viewbox Width="50" Height="50">
            <ContentPresenter Content="{Binding}"/>
          </Viewbox>
          
        </StackPanel>
      </GroupBox>

    </StackPanel>
    <StackPanel Grid.Column="2">
      <GroupBox Header="root" DataContext="{Binding MyData}">
        <StackPanel>
          <TextBlock Text="{Binding SelectedItemsData.Count, StringFormat='選択Item個数 = {0:0}'}"/>
        </StackPanel>
      </GroupBox>
      <!--<GroupBox Header="MyClickedItem" DataContext="{Binding MyData.MyClickedItem}">
        <StackPanel>
          --><!--<ContentPresenter Content="{Binding ElementName=MyRootItems}"/>--><!--
          <TextBlock Text="{Binding ActualHeight}"/>
          <TextBlock Text="{Binding Height}"/>
          <Rectangle Width="50" Height="50">
            <Rectangle.Fill>
              <VisualBrush Visual="{Binding}" Stretch="Uniform"/>
            </Rectangle.Fill>
          </Rectangle>
        </StackPanel>
      </GroupBox>-->

      <GroupBox>
        <StackPanel>
          <TextBlock Text="{Binding MyData.DataList[0].IsSelected, StringFormat='isSelected0 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[0].IsSelectable, StringFormat='IsSelectable0 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[0], StringFormat='data = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[1].IsSelected, StringFormat='isSelected1 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[1].IsSelectable, StringFormat='IsSelectable1 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[1], StringFormat='data = {0}'}"/>
        </StackPanel>
      </GroupBox>
      <ListBox ItemsSource="{Binding MyData.SelectedItemsData}" IsHitTestVisible="False">
        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal"/>
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Viewbox Height="50" Width="50">
              <ContentPresenter Content="{Binding}"/>
            </Viewbox>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </StackPanel>
  </Grid>
</Window>