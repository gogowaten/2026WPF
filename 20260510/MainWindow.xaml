<Window x:Class="_20260510.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260510" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="867" Width="854">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top">
      <local:RootItemsControl x:Name="MyRootItems"
                              UseLayoutRounding="True"
                              MyData="{Binding MyData}"
                              />

    </ScrollViewer>

    <Canvas x:Name="MyCanvas">
      <local:GeoLineEX MyPoints="0,0 100,10" Stroke="Red" StrokeThickness="40" MyBackground="Beige"
                       Canvas.Left="40" Canvas.Top="20"/>
    </Canvas>

    <StackPanel Grid.Column="1">
      <CheckBox Content="選択状態枠表示" IsChecked="{Binding MyData.IsVisbleSelectedBorder}"/>
      <CheckBox Content="Group枠表示" IsChecked="{Binding MyData.IsVisbleGroupBorder}"/>
      <GroupBox Header="Data追加時のZモード">
        <GroupBox.Resources>
          <local:MyConvEnumToBool x:Key="EnumToBool"/>
        </GroupBox.Resources>
        <UniformGrid Rows="3" Columns="2">
          <RadioButton Content="Currentの上"
                       IsChecked="{Binding MyData.ShiftZIndexMode, Converter={StaticResource EnumToBool},
            ConverterParameter=Upper}"/>
          <RadioButton Content="一番上"
                       IsChecked="{Binding MyData.ShiftZIndexMode, Converter={StaticResource EnumToBool},
            ConverterParameter=Top}"/>
          <RadioButton Content="Currentの下"
                       IsChecked="{Binding MyData.ShiftZIndexMode, Converter={StaticResource EnumToBool},
            ConverterParameter=Under}"/>
          <!--<RadioButton Content="一番下"
                       IsChecked="{Binding MyData.ShiftZIndexMode, Converter={StaticResource EnumToBool},
            ConverterParameter=Bottom}"/>-->
          <RadioButton Content="一番下"
                       IsChecked="{Binding MyData.ShiftZIndexMode, Converter={StaticResource EnumToBool},
            ConverterParameter={x:Static local:ModeAddZIndex.Bottom}}"/>
          <TextBlock Text="{Binding MyData.ShiftZIndexMode, StringFormat='ZMode = {0}'}"/>
        </UniformGrid>
      </GroupBox>
      <Button x:Name="check" Content="check" Click="check_Click"/>
      <Button Content="グループ化" Command="{Binding MyData.AddGroupFromSelectedItemsCommand}"/>
      <Button Content="グループ解除" Command="{Binding MyData.UnGroupCurrentCommand}"/>
      <Button Content="ContextMenu" Click="ContextMenu_Click"/>
      <Button Content="ZDown" Command="{Binding MyData.ZDownSelectedItemsCommand}"/>
      <Button Content="ZupCommand" Command="{Binding MyData.ZUpSelectedItemsCommand}"/>
      <Button Content="Currentを編集モードに移行" Command="{Binding MyData.EditingCurrentGroupCommand}"/>
      <Button Content="Clickedを編集モードに移行" Command="{Binding MyData.MigreteEditingFromClickedCommand}"/>
      <Button Content="1個上を編集する" Command="{Binding MyData.EditingUpperGroupCommand}"/>
      <Button Content="全体をpng画像で保存" Command="{Binding MyData.SaveRootToPngImageFileCommand}"/>
      <!--<Button Content="全体をpng画像で保存" Click="ButtonSaveRootToPngImage_Click"/>-->
      <Button Content="currentを画像保存" Command="{Binding MyData.SaveCurrentItemToPngImageFileCommand}"/>
      <Button Content="Test追加Rect" Click="ButtonTestAddRect_Click"/>

      <GroupBox Header="CurrentData" DataContext="{Binding MyData.CurrentItemData}">
        <StackPanel>
          <TextBlock Text="{Binding Name}"/>
          <TextBlock Text="{Binding Width, StringFormat='Width = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Height, StringFormat='Height = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding X, StringFormat='X = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Y, StringFormat='Y = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding Z, StringFormat='Z = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding ParentData, StringFormat='ParentData = {0: 0; -0; 0}'}"/>

          <TextBlock Text="{Binding StrokeThickness, StringFormat='StrokeThickness = {0: 0; -0; 0}'}"/>
          <Slider Value="{Binding StrokeThickness}" Minimum="1" Maximum="50"
                  SmallChange="10" LargeChange="10"/>
          <Viewbox Width="50" Height="50" Stretch="Uniform">
            <ContentControl Content="{Binding}"/>
          </Viewbox>

        </StackPanel>
      </GroupBox>

    </StackPanel>
    <StackPanel Grid.Column="2">
      <GroupBox Header="root" DataContext="{Binding MyData}">
        <StackPanel>
          <TextBlock Text="{Binding SelectedItemsData.Count, StringFormat='選択Item個数 = {0:0}'}"/>
        </StackPanel>
      </GroupBox>
      
      <GroupBox Header="ClickedItemData" DataContext="{Binding MyData.ClickedItemData}">
        <StackPanel>
          <TextBlock Text="{Binding Name}"/>
          <Viewbox Stretch="Uniform" Width="50" Height="50">
            <ContentPresenter Content="{Binding}"/>
          </Viewbox>
        </StackPanel>
      </GroupBox>
      
      <!--<GroupBox Header="MyClickedItem" DataContext="{Binding MyData.MyClickedItem}">
        <StackPanel>
          -->
      <!--<ContentPresenter Content="{Binding ElementName=MyRootItems}"/>-->
      <!--
          <TextBlock Text="{Binding ActualHeight}"/>
          <TextBlock Text="{Binding Height}"/>
          <Rectangle Width="50" Height="50">
            <Rectangle.Fill>
              <VisualBrush Visual="{Binding}" Stretch="Uniform"/>
            </Rectangle.Fill>
          </Rectangle>
        </StackPanel>
      </GroupBox>-->

      <GroupBox>
        <StackPanel>
          <TextBlock Text="{Binding MyData.DataList[0].IsSelected, StringFormat='isSelected0 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[0].IsSelectable, StringFormat='IsSelectable0 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[0], StringFormat='data = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[1].IsSelected, StringFormat='isSelected1 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[1].IsSelectable, StringFormat='IsSelectable1 = {0}'}"/>
          <TextBlock Text="{Binding MyData.DataList[1], StringFormat='data = {0}'}"/>
        </StackPanel>
      </GroupBox>

      <Button Content="全削除" Command="{Binding MyData.RemoveAllCommand}"/>
      <Button Content="選択Itemを削除" Command="{Binding MyData.RemoveSelectedItemsCommand}"/>
      <ListBox ItemsSource="{Binding MyData.SelectedItemsData}" IsHitTestVisible="False">
        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal"/>
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Viewbox Height="50" Width="50">
              <ContentPresenter Content="{Binding}"/>
            </Viewbox>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>


      <TreeView ItemsSource="{Binding MyData.DataList}">
        <TreeView.ItemContainerStyle>
          <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
          </Style>
        </TreeView.ItemContainerStyle>
        <TreeView.ItemTemplate>
          <HierarchicalDataTemplate DataType="{x:Type local:GroupData}" ItemsSource="{Binding DataList}">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="{Binding Z, StringFormat='Z={0:0 }'}"/>
              <TextBlock Text="{Binding Name}"/>
            </StackPanel>
          </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
      </TreeView>
    </StackPanel>
  </Grid>
</Window>