<Window x:Class="_20260321.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260321" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <!--<ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Left" VerticalAlignment="top">
    </ScrollViewer>-->
    <Canvas x:Name="MyCanvas">
      <Polyline x:Name="MyLine0" Canvas.Left="100" Canvas.Top="100"
                  Stroke="MediumOrchid" StrokeThickness="100"
                  Points="0,0 200,0"/>

      <Canvas Canvas.Left="100" Canvas.Top="100" Width="{Binding ElementName=MyLine, Path=ActualWidth}"
              Height="{Binding ElementName=MyLine, Path=ActualHeight}" RenderTransformOrigin="0.5,0.5">
        <Canvas.RenderTransform>
            <RotateTransform Angle="25"/>
        </Canvas.RenderTransform>
        <Polyline x:Name="MyLine" Canvas.Left="0" Canvas.Top="0"
                  Stroke="MediumAquamarine" StrokeThickness="100"
                  Points="0,0 200,0"/>
      </Canvas>


    </Canvas>


    <StackPanel Grid.Column="1" DataContext="{Binding ElementName=MyLine}">
      <TextBlock Text="{Binding DesiredSize, StringFormat='DesiredSize = {0:0.0}'}"/>
      <TextBlock Text="{Binding RenderSize, StringFormat='RenderSize = {0:0.0}'}"/>
      <TextBlock Text="{Binding ActualWidth, StringFormat='ActWidth = {0:0.0}'}"/>
      <TextBlock Text="{Binding Width, StringFormat='Width = {0:0.0}'}"/>
      <TextBlock Text="{Binding ActualHeight, StringFormat='ActHeight = {0:0.0}'}"/>
      <TextBlock Text="{Binding Height, StringFormat='Height = {0:0.0}'}"/>
      <Button Content="test" Click="Button_Click_1"/>
    </StackPanel>
  </Grid>

</Window>