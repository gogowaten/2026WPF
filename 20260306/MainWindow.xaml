<Window x:Class="_20260306.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260306" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  
  <Window.Resources>
    <Style x:Key="CanvasItemContainerStyle" TargetType="ContentPresenter">
      <Setter Property="Canvas.Left" Value="{Binding X}" />
      <Setter Property="Canvas.Top" Value="{Binding Y}" />
      <!--<Setter Property="Panel.ZIndex" Value="{Binding Z}" />-->
    </Style>
    
    <DataTemplate DataType="{x:Type local:GroupData}">
      <ItemsControl ItemsSource="{Binding DataList}"
                    ItemContainerStyle="{StaticResource CanvasItemContainerStyle}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas IsItemsHost="True"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <!--<ItemsControl.ItemTemplate>
          <DataTemplate>
            <ContentControl>
              <ContentControl.Template>
                <ControlTemplate>
                  <ContentPresenter/>
                </ControlTemplate>
              </ContentControl.Template>
            </ContentControl>
          </DataTemplate>
        </ItemsControl.ItemTemplate>-->
        
      </ItemsControl>
    </DataTemplate>

    <!--<DataTemplate x:Key="aa">
      <ContentControl>
        <ContentControl.Style>
          <Style TargetType="{x:Type ContentControl}">
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                  <ContentPresenter/>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ContentControl.Style>
      </ContentControl>
    </DataTemplate>-->
      
    <DataTemplate DataType="{x:Type local:RectangleData}">
      <Rectangle Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Fill}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:EllipseData}">
      <Ellipse Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Fill}"/>
    </DataTemplate>
  </Window.Resources>
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas">
      <!--<ItemsControl ItemsSource="{Binding MyGroupData.DataList}"
                    ItemContainerStyle="{StaticResource CanvasItemContainerStyle}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas IsItemsHost="True"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
          <ItemContainerTemplate>
            <ItemsControl ItemsSource="{Binding}"
                          ItemContainerStyle="{StaticResource CanvasItemContainerStyle}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <Canvas IsItemsHost="True"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>

              <ItemsControl.ItemTemplate>
                <ItemContainerTemplate>
                  <ContentPresenter/>
                </ItemContainerTemplate>
              </ItemsControl.ItemTemplate>
              
            </ItemsControl>
          </ItemContainerTemplate>
        </ItemsControl.ItemTemplate>
        
      </ItemsControl>-->
      
     
      
      <ItemsControl ItemsSource="{Binding MyGroupData.DataList}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas IsItemsHost="True"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <ItemsControl.ItemContainerStyle>
          <Style>
            <Setter Property="Canvas.Left" Value="{Binding X}"/>
            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
          </Style>
        </ItemsControl.ItemContainerStyle>
      </ItemsControl>
      
    </Canvas>

    
    <StackPanel Grid.Column="1">
      <ListBox ItemsSource="{Binding MyGroupData.DataList}">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal" Height="20">
                <Viewbox>
                  <ContentControl Content="{Binding}"/>
                </Viewbox>
                <TextBlock Text="{Binding X, StringFormat='Left = {0:0.0}'}"/>
                <TextBlock Text="{Binding Y, StringFormat=', Top = {0:0.0}'}"/>
            </StackPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <Slider Minimum="0" Maximum="200" Value="{Binding MyGroupData.DataList[0].X}"/>

      <TreeView ItemsSource="{Binding MyGroupData.DataList}">
        
      </TreeView>
    </StackPanel>
  </Grid>
</Window>
