<Window x:Class="_20260704.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260704"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas" UseLayoutRounding="True">
      <TextBlock x:Name="MyElement" Text="test" FontSize="50" Background="Red"/>
      <Rectangle>
        
      </Rectangle>
      <!--<Border Name="MyElement" Canvas.Left="20" Canvas.Top="20" BorderBrush="Gray" BorderThickness="10">
        <TextBlock x:Name="MyTextBlock" Text="test" Background="Red" FontSize="50">
          <TextBlock.LayoutTransform>
            <RotateTransform Angle="20"/>
          </TextBlock.LayoutTransform>
        </TextBlock>
      </Border>-->

      <Border x:Name="MyBorder" Canvas.Left="10" Canvas.Top="20"
              BorderBrush="Beige">
        <Image x:Name="MyImage">
          <Image.LayoutTransform>
            <RotateTransform Angle="29"/>
          </Image.LayoutTransform>
        </Image>
      </Border>
    </Canvas>

    <StackPanel Grid.Column="1">
      <Button Content="DrawTest" Click="Button_Click"/>
      <Button Content="DrawTestGDI" Click="Button_Click_1"/>
    </StackPanel>
  </Grid>
</Window>
