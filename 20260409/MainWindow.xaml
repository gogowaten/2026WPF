<Window x:Class="_20260409.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260409" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas">

      <!--<Path x:Name="MyShape" DataContext="{Binding MyPG}" Stroke="{Binding Stroke}" StrokeThickness="{Binding StrokeThickness}" Data="{Binding MyGeometry}"/>-->
      
      <!--<local:GeoThumb x:Name="MyShape" DataContext="{Binding MyPG}"/>-->
      
      <local:DataThumb x:Name="MyShape" DataContext="{Binding MyPG}"/>
    </Canvas>

    <StackPanel Grid.Column="1">
      <Button Content="Check" Click="Button_Click"/>
      <Slider Value="{Binding MyPG.StrokeThickness}" SmallChange="20" LargeChange="20" Minimum="20" Maximum="100"/>
      <!--<Button Content="offset" Command="{Binding MyPG.OffsetCommand}"/>-->
      <Button Content="addPoint" Click="Button_Click_Add"/>
      <GroupBox DataContext="{Binding MyPG}">
        <StackPanel>
          <TextBlock Text="{Binding StrokeThickness, StringFormat='StrokeThickness = {0}'}"/>
          <TextBlock Text="{Binding Width, StringFormat='Width = {0}'}"/>
          <TextBlock Text="{Binding Height, StringFormat='Height = {0}'}"/>
          <TextBlock Text="{Binding InternalX, StringFormat='InternalX = {0}'}"/>
          <TextBlock Text="{Binding X, StringFormat='X = {0}'}"/>
          <TextBlock Text="{Binding BoundsWidth, StringFormat='BoundsWidth = {0}'}"/>
          <TextBlock Text="{Binding MyPoints, StringFormat='Points = {0: 0; -0; 0}'}"/>
        </StackPanel>
      </GroupBox>
    </StackPanel>
  </Grid>
</Window>
