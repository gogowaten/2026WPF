<Window x:Class="_20260409.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260409" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <!--<Canvas x:Name="MyCanvas">
      <Path Stroke="Red" StrokeThickness="10">
        <Path.Data>
          <PathGeometry>
            <PathGeometry.Figures>
              <PathFigure StartPoint="0,0">
                <PolyLineSegment Points="100,0"/>
              </PathFigure>
            </PathGeometry.Figures>
          </PathGeometry>
        </Path.Data>
      </Path>-->

      <Path x:Name="MyShape" Stroke="{Binding MyPG.Stroke}" StrokeThickness="{Binding MyPG.StrokeThickness}" Data="{Binding MyPG.MyGeometry}"/>
      <!--<Path x:Name="MyShape" Stroke="Blue" StrokeThickness="20" Data="{Binding MyPG}"/>--><!--
    </Canvas>-->

    <StackPanel Grid.Column="1">
      <Button Content="Check" Click="Button_Click"/>
      <Slider Value="{Binding MyPG.StrokeThickness}" SmallChange="20" LargeChange="20" Minimum="20" Maximum="100"/>
    </StackPanel>
  </Grid>
</Window>
