<Window x:Class="_20260324.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260324" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Window.Resources>
    <!--<DataTemplate DataType="{x:Type local:MyData}">
      <Thumb>
        <Thumb.Template>
          <ControlTemplate TargetType="{x:Type Thumb}">
            <Border BorderBrush="DeepSkyBlue" BorderThickness="1">
              <local:GeoLine Points="{Binding Points}" Canvas.Left="{Binding XP}" Canvas.Top ="{Binding YP}"
                     Stroke="{Binding Brush}" StrokeThickness="{Binding StrokeThickness}"/>
            </Border>
          </ControlTemplate>
        </Thumb.Template>
      </Thumb>
    </DataTemplate>-->
  </Window.Resources>
  <!--<Window.DataContext>
    <local:MyData/>
  </Window.DataContext>-->

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <!--<ContentControl Content="{Binding}"/>-->

    <!--<local:GeoLine x:Name="MyLine"
                   Points="50,70 250,150 50,250 50,200 50,150 150,100 250,250"
                   Stroke="Gold" StrokeThickness="20"
                   />-->
    <local:GeoLineThumb x:Name="MyGeoLineThumb" DataContext="{Binding MyData}"
                        />
    <Canvas x:Name="MyCanvas">

    </Canvas>

    <StackPanel Grid.Column="1" DataContext="{Binding MyData}">
    <!--<StackPanel Grid.Column="1" DataContext="{Binding ElementName=MyLine}">-->
      <Button Content="test" Click="Button_Click"/>
      <TextBlock Text="{Binding BoundsWidth, StringFormat='BoundsWidth = {0:0}'}"/>
      <TextBlock Text="{Binding BoundsHeight, StringFormat='BoundsHeight = {0:0}'}"/>
      <TextBlock Text="{Binding LinePen.Thickness, StringFormat='Thickness = {0:0}'}"/>
      <Slider Value="{Binding StrokeThickness}" Minimum="1" Maximum="40"/>
    </StackPanel>
  </Grid>
</Window>
