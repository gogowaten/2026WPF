<Window x:Class="_20260413.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260413"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas">
      <local:CanvasThumb x:Name="MyCanvasThumb"
                         Canvas.Left="150" Canvas.Top="30"
                         Width="100" Height="100"/>
    </Canvas>

    <StackPanel Grid.Column="1">
      <Button Content="AddResizeHnadle" Click="Button_Click_Add"/>
      <Button Content="RemoveResizeHnadle" Click="Button_Click_Remove"/>
      <GroupBox Header="CanvasThumb" DataContext="{Binding ElementName=MyCanvasThumb}">
        <StackPanel>
          <TextBlock Text="{Binding ActualHeight, StringFormat='ActualHeight ={0:0}'}"/>
          <TextBlock Text="{Binding Height, StringFormat='Height ={0:0}'}"/>
          <TextBlock Text="{Binding MyResizeAdorner.ResizeHandleSize}"/>
          <Slider Value="{Binding MyResizeAdorner.ResizeHandleSize}" Minimum="10" Maximum="50"/>
        </StackPanel>
      </GroupBox>

    </StackPanel>
  </Grid>
</Window>