<Window x:Class="_20260309.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260309" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <!--<Window.Resources>
    <Style x:Key="CanvasItemContainer">
      <Setter Property="Canvas.Left" Value="{Binding X}"/>
      <Setter Property="Canvas.Top" Value="{Binding Y}"/>
    </Style>
    
    <DataTemplate DataType="{x:Type local:RectangleData}">
      <Rectangle Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Fill}"/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:EllipseData}">
      <Ellipse Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Fill}"/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:GroupData}">
      <ItemsControl ItemsSource="{Binding DataList}"
                    ItemContainerStyle="{StaticResource CanvasItemContainer}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas IsItemsHost="True"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
      </ItemsControl>
    </DataTemplate>
    
  </Window.Resources>-->
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <!--<local:AAA x:Name="MyAAA" ItemsSource="{Binding MyRootData.DataList}"/>-->
    <local:AAA x:Name="MyAAA" DataContext="{Binding MyRootData}"/>

    <!--<ItemsControl ItemsSource="{Binding MyRootData.DataList}"
                  ItemContainerStyle="{StaticResource CanvasItemContainer}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <Canvas IsItemsHost="True"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
    </ItemsControl>-->
    <Canvas x:Name="MyCanvas">

    </Canvas>

    <StackPanel Grid.Column="1">
      <Button Content="test" Click="Button_Click"/>
      <Button Content="右へ10移動" Command="{Binding }"/>
    </StackPanel>
  </Grid>
</Window>
