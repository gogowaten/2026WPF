<Window x:Class="_20260221_AutoSizePanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260221_AutoSizePanel"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Grid HorizontalAlignment="Left" VerticalAlignment="Top">
      <local:NodePanel x:Name="MyRoot" Background="AliceBlue" Margin="0" DockPanel.Dock="Left">
        <Rectangle x:Name="MyRect1" Fill="Orange" Width="100" Height="100"
                 local:NodePanel.X="10" local:NodePanel.Y="10"
                 Opacity="0.5"/>
        <Rectangle x:Name="MyRect2" Fill="DodgerBlue"
                   Width="100" Height="100"
                 local:NodePanel.X="200" local:NodePanel.Y="50"
                 Opacity="0.5"/>
      </local:NodePanel>
    </Grid>

    <StackPanel Grid.Column="1" DataContext="{Binding ElementName=MyRoot}">
      <TextBlock Text="{Binding ActualWidth, StringFormat='PanelActualW = {0}'}"/>
      <TextBlock Text="{Binding Width, StringFormat='PanelWidth = {0}'}"/>
    </StackPanel>
  </Grid>
</Window>

