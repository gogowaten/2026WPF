<Window x:Class="_20260224_DataTemplate3_Behavior.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260224_DataTemplate3_Behavior"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Window.DataContext>
    <local:MainViewModel/>
  </Window.DataContext>

  <Window.Resources>
    <DataTemplate DataType="{x:Type local:Items}">
      <Canvas>
        <ItemsControl ItemsSource="{Binding Children}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <Canvas/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
              <Setter Property="Canvas.Left" Value="{Binding X}"/>
              <Setter Property="Canvas.Top" Value="{Binding Y}"/>
            </Style>
          </ItemsControl.ItemContainerStyle>
        </ItemsControl>
      </Canvas>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:TextBlockItem}">
      <Border Background="Snow" BorderBrush="DarkGreen" BorderThickness="2" CornerRadius="5" Padding="10,5">
        <TextBlock Text="{Binding Text}" Foreground="Red" FontWeight="Bold">
          <i:Interaction.Behaviors>
            <local:MouseDragBehavior/>
          </i:Interaction.Behaviors>
        </TextBlock>
      </Border>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:RectangleItem}">
      <Rectangle Width="{Binding Width}" Height="{Binding Height}" Fill="{Binding Fill}">
        <i:Interaction.Behaviors>
          <local:MouseDragBehavior/>
        </i:Interaction.Behaviors>
      </Rectangle>
    </DataTemplate>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Grid x:Name="MyGrid" Background="YellowGreen">
      <ItemsControl ItemsSource="{Binding RootItems}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
          <Style TargetType="ContentPresenter">
            <Setter Property="Canvas.Left" Value="{Binding X}"/>
            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
          </Style>
        </ItemsControl.ItemContainerStyle>
      </ItemsControl>
    </Grid>

    <StackPanel Grid.Column="1">
      
      <!--<Button Content="Add" Click="Button_Click"/>-->
    </StackPanel>
  </Grid>
</Window>