<Window x:Class="_20260428.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260428" d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top">
      <local:RootItemsControl x:Name="MyRootItems"
                              UseLayoutRounding="True"
                              MyData="{Binding MyData}"/>
      
    </ScrollViewer>
    <!--<Canvas x:Name="MyCanvas">

    </Canvas>-->
    <!--<Polyline x:Name="MyLine" Points="100,30 250,130" Stroke="Red" StrokeThickness="60"/>-->
    <!--<local:GeoLine x:Name="MyLine" MyPoints="100,30 250,130" Stroke="Red" StrokeThickness="60"/>-->

    <StackPanel Grid.Column="1">
      <GroupBox Header="MyData.ClickedItemData" DataContext="{Binding MyData.ClickedItemData}">
        <StackPanel>
          <TextBlock Text="{Binding Width, StringFormat='Width = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding X, StringFormat='X = {0: 0; -0; 0}'}"/>
          <!--<TextBlock Text="{Binding InternalX, StringFormat='InternalX = {0: 0; -0; 0}'}"/>-->
          <TextBlock Text="{Binding OffsetX, StringFormat='OffsetX = {0: 0; -0; 0}'}"/>
          <TextBlock Text="{Binding OffsetY, StringFormat='OffsetY = {0: 0; -0; 0}'}"/>

          <Button Content="test" Click="Button_Click"/>
        </StackPanel>
      </GroupBox>
      <GroupBox Header="RootThumb" DataContext="{Binding ElementName=MyRootItems}">
        <StackPanel>
          <TextBlock Text="{Binding ActualWidth, StringFormat='ActualWidth = {0}'}"/>
          <TextBlock Text="{Binding (Canvas.Left), StringFormat='Canvas.Left = {0}'}"/>
        </StackPanel>
      </GroupBox>
    </StackPanel>
  </Grid>
</Window>