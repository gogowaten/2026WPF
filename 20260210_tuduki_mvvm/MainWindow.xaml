<Window x:Class="_20260210_tuduki_mvvm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260210_tuduki_mvvm"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Window.DataContext>
    <local:MainViewModel/>
  </Window.DataContext>
  <Window.Resources>
    <Style TargetType="{x:Type local:DraggableRectangle}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type local:DraggableRectangle}">
            <Rectangle x:Name="rect" Fill="LightBlue" Stroke="Blue" StrokeThickness="2"
                       Width="{TemplateBinding Width}"
                       Height="{TemplateBinding Height}"/>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="rect" Property="StrokeThickness" Value="3"/>
                <Setter TargetName="rect" Property="Stroke" Value="Orange"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>
    
    <ItemsControl ItemsSource="{Binding Rects}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <Canvas Background="#f0f0f0"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      
      <ItemsControl.ItemContainerStyle>
        <Style TargetType="ContentPresenter">
          <Setter Property="Canvas.Left" Value="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
          <Setter Property="Canvas.Top" Value="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Style>
      </ItemsControl.ItemContainerStyle>
      
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <local:DraggableRectangle Width="{Binding Width, Mode=TwoWay}" Height="{Binding Height, Mode=TwoWay}">
            <local:DraggableRectangle.Template>
              <ControlTemplate TargetType="local:DraggableRectangle">
                <Rectangle Fill="SkyBlue"
                           Stroke="Blue"
                           StrokeThickness="1"/>
              </ControlTemplate>
            </local:DraggableRectangle.Template>
          </local:DraggableRectangle>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    
    
    
    <Canvas x:Name="MainCanvas" Background="Transparent" Visibility="Collapsed"
            MouseDown="MainCanvas_MouseDown"
            MouseMove="MainCanvas_MouseMove"
            MouseUp="MainCanvas_MouseUp">
      <Rectangle x:Name="selectionBox" Visibility="Collapsed"
                 Stroke="DodgerBlue" StrokeThickness="1" StrokeDashArray="3 1"
                 Fill="#331e90ff" Panel.ZIndex="999"/>
      <local:DraggableRectangle Canvas.Left="10" Canvas.Top="10" Width="150" Height="100"/>
      <local:DraggableRectangle Canvas.Left="100" Canvas.Top="100" Width="50" Height="100"/>
    </Canvas>

    <StackPanel Grid.Column="1">
      <Button x:Name="Save" Content="保存" Command="{Binding SaveCommand}"/>
      <Button Content="読み込み" Command="{Binding LoadCommand}"/>
      
      <Button x:Name="AddItem" Content="追加" Command="{Binding AddRectCommand}"/>
      <!--<Button x:Name="RemoveItem" Content="削除" Command="{Binding }"/>-->
    </StackPanel>
  </Grid>
</Window>