02からの付け足し<br>

* 済：CanvasThumbに、VertexAdornerを付け加えたGeoThumbを入れる	
* 済：ハンドルThumbのカスタムコントロール化	
* 済：CanvasThumbの中の要素は一個だけなので、フィールドで保持	
* 済：DataクラスとGeoThumbに頂点ハンドル表示切り替えプロパティの設置してバインド	
* 済：頂点ハンドルの更新、これは手動になってしまったけど、これでいいかも	
* 保留：VertexAdornerでadornedElementから取り出したGeoLineからPointsを取り出してフィールドで扱って、Points更新時に個数が増減していたらハンドル更新	
   	* これはMyPointsをObservableCollectionに変更する必要があるし、手動で行うのも悪くない気がするから保留
* 今日、2026/04/23はここまで	
* リサイズハンドルや頂点ハンドルのサイズDataはどこに持たせるのが良い？	
* 保留：要素に持たせるとバラバラのサイズになるからアプリに持たせたい、そして、それを各要素とバインド	
* 済：リサイズハンドルもカスタムコントロールにする、背景色を指定できるようにする	
* 済：手動でぴったりサイズ、Canvasサイズを図形がピッタリ収まるサイズにする	
	* どこに持たせるのが良い？図形じゃない、GeoThumbも違う気がする、ってことはCanvasThumb
