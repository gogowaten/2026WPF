<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:_20260420">


  <Style TargetType="{x:Type local:CanvasThumb}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:CanvasThumb}">
          <Canvas x:Name="PART_Canvas" Background="Red">
            <local:GeoThumb/>
          </Canvas>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>



  <Style TargetType="{x:Type local:GeoThumb}">
    <Setter Property="Canvas.Left" Value="{Binding InternalX, Mode=TwoWay}"/>
    <Setter Property="Canvas.Top" Value="{Binding InternalY, Mode=TwoWay}"/>
    
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:GeoThumb}">
         
            <local:GeoLine x:Name="PART_GeoLine"
                           MyPoints="{Binding MyPoints}"
                           Fill="{Binding Fill}"
                           Stroke="{Binding Stroke}"
                           StrokeThickness="{Binding StrokeThickness}"
                           StrokeEndLineCap="{Binding EndLineCap}"
                           StrokeStartLineCap="{Binding StartLineCap}"
                           StrokeLineJoin="{Binding LineJoin}"
                           StrokeMiterLimit="{Binding MiterLimit}"
                               />
     
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

                           <!--MyGeometryBounds="{Binding GeometryBounds, Mode=OneWayToSource}"-->
                           <!--MyIsOffset="{Binding IsOffset}"-->
                           <!--MyStrokePen="{Binding StrokePen, Mode=OneWayToSource}"-->


  <!--<Style TargetType="{x:Type local:GeoThumb}">
    <Setter Property="Canvas.Left" Value="{Binding InternalX}"/>
    <Setter Property="Canvas.Top" Value="{Binding InternalY}"/>
    
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:GeoThumb}">
          <Border Background="MediumSlateBlue"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Width="{Binding ElementName=PART_GeoLine, Path=MyGeometryBounds.Width, Mode=OneWayToSource}"
                  Height="{Binding ElementName=PART_GeoLine, Path=MyGeometryBounds.Height, Mode=OneWayToSource}"
                  >
            <local:GeoLine x:Name="PART_GeoLine"
                           MyPoints="{Binding MyPoints}"
                           Fill="{Binding Fill}"
                           Stroke="{Binding Stroke}"
                           StrokeThickness="{Binding StrokeThickness}"
                           StrokeEndLineCap="{Binding EndLineCap}"
                           StrokeStartLineCap="{Binding StartLineCap}"
                           StrokeLineJoin="{Binding LineJoin}"
                           StrokeMiterLimit="{Binding MiterLimit}"
                           MyIsOffset="{Binding IsOffset}"
                           MyStrokePen="{Binding StrokePen}"
                           MyGeometryBounds="{Binding GeometryBounds, Mode=TwoWay}"
                               />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>-->






</ResourceDictionary>