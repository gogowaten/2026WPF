<Window x:Class="_20260524_01_ElementToBitmap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260524_01_ElementToBitmap"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer x:Name="MyScroll"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  PreviewMouseLeftButtonDown="MyScroll_PreviewMouseLeftButtonDown"
                  MouseLeftButtonUp="MyScroll_MouseLeftButtonUp"
                  MouseMove="MyScroll_MouseMove">
      <Image x:Name="ImageControl"
             Stretch="None"
             SnapsToDevicePixels="True"
             RenderOptions.BitmapScalingMode="NearestNeighbor"
             UseLayoutRounding="True"
             RenderOptions.EdgeMode="Aliased"
             >
        <Image.LayoutTransform>
          <TransformGroup>
            <RotateTransform x:Name="MyRotate" Angle="20"/>
            <ScaleTransform ScaleX="{Binding MyImageScale}"
                            ScaleY="{Binding MyImageScale}"/>
          </TransformGroup>
        </Image.LayoutTransform>
      </Image>
    </ScrollViewer>

    <StackPanel Grid.Column="1">
      <StackPanel.Resources>
        <Style TargetType="TextBlock">
          <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="GroupBox">
          <Setter Property="Margin" Value="4"/>
        </Style>
        <Style TargetType="Button">
          <Setter Property="Margin" Value="15,4"/>
        </Style>
      </StackPanel.Resources>
      
      <Button Content="コピー" Click="ButtonCopyToClipboard_Click"/>
      <Button Content="コピー（変形後）" Click="Button_Click"/>
      <Button Content="保存" Click="ButtonSave_Click"/>
      <Button Content="保存(変形後)" Click="ButtonSaveExterior_Click"/>
      <Button x:Name="ButtonNaname" Content="斜めボタン" Click="ButtonNaname_Click">
        <Button.LayoutTransform>
          <TransformGroup>
            <RotateTransform Angle="30"/>
            <ScaleTransform ScaleX="2" ScaleY="2"/>
          </TransformGroup>
        </Button.LayoutTransform>
      </Button>
      <Separator/>

      <TextBlock Text="{Binding MyImageScale, StringFormat='表示倍率 = {0:0.0}'}"/>
      <Slider x:Name="ImageScale" Minimum="1" Maximum="30" Value="{Binding MyImageScale}"
              SmallChange="1" LargeChange="1"
              Margin="10,5" TickFrequency="1.0"/>
      <UniformGrid Columns="4" Rows="2">
        <UniformGrid.Resources>
          <Style TargetType="Button">
            <Setter Property="Margin" Value="2"/>
          </Style>
        </UniformGrid.Resources>
        <Button Content="1" Tag="1" Click="ButtonSetScale_Click"/>
        <Button Content="3" Tag="3" Click="ButtonSetScale_Click"/>
        <Button Content="10" Tag="10" Click="ButtonSetScale_Click"/>
        <Button Content="50" Tag="50" Click="ButtonSetScale_Click"/>
        <Button Content="x 2" Tag="2" Click="ButtonSetMathScale_Click"/>
        <Button Content="x 10" Tag="10" Click="ButtonSetMathScale_Click"/>
        <Button Content="x 0.5" Tag="0.5" Click="ButtonSetMathScale_Click"/>
        <Button Content="x 0.1" Tag="0.1" Click="ButtonSetMathScale_Click"/>
      </UniformGrid>

      <Separator/>

      <TextBlock Text="{Binding ElementName=MyRotate, Path=Angle, StringFormat='角度 = {0:000}度'}"/>
      <Slider Minimum="0" Maximum="360" Value="{Binding ElementName=MyRotate, Path=Angle}"
              SmallChange="10" LargeChange="10" TickFrequency="10"/>
    </StackPanel>
  </Grid>
</Window>