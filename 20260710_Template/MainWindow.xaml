<Window x:Class="_20260710_Template.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20260710_Template"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <!--Title="MainWindow" Height="{Binding WindowHeight}" Width="{Binding WindowWidth}">-->
  <Window.DataContext>
    <local:Data/>
    <!--<local:MainViewModel/>-->
  </Window.DataContext>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas">

    </Canvas>

    <StackPanel Grid.Column="1">
      <GroupBox>
        <GroupBox.DataContext>
          <local:MainWindow/>
        </GroupBox.DataContext>
        <StackPanel>
          <!--<TextBlock Text="{Binding Width, StringFormat='width = {0}'}"/>-->
          <!--<TextBlock Text="{Binding ActualWidth, StringFormat='actwidth = {0}'}"/>-->
          <!--<TextBlock Text="{Binding RestoreBounds.Width, StringFormat='actwidth = {0}'}"/>
          <TextBlock Text="{Binding RestoreBounds.Width}"/>-->
        </StackPanel>
      </GroupBox>
    </StackPanel>
  </Grid>
</Window>
