<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:_20260403">





  <!--<DataTemplate DataType="{x:Type local:GeoLineData}">
    <local:GeoLine x:Name="GeoLine"
                   
                   MyPoints="{Binding Points}"
                   Stroke="{Binding Stroke}"
                   StrokeThickness="{Binding StrokeThickness}"
                   Fill="{Binding Fill}"
                   StrokeEndLineCap="{Binding EndLineCap}"
                   StrokeStartLineCap="{Binding StartLineCap}"
                   StrokeLineJoin="{Binding LineJoin}"
                   StrokeMiterLimit="{Binding MiterLimit}"
                   MyIsOffset="{Binding IsOffset}"
                   />
  </DataTemplate>-->

  <Style TargetType="{x:Type local:CustomThumb}">
    <Setter Property="MyData" Value="{Binding}"/>
    <Setter Property="Canvas.Left" Value="{Binding X}"/>
    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
    <Setter Property="IsCanDragMove" Value="{Binding IsCanDragMove}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:CustomThumb}">
          <Canvas>
            <local:GeoLine x:Name="GeoLine"
                           
                           MyPoints="{Binding Points}"
                           Stroke="{Binding Stroke}"
                           StrokeThickness="{Binding StrokeThickness}"
                           Fill="{Binding Fill}"
                           StrokeEndLineCap="{Binding EndLineCap}"
                           StrokeStartLineCap="{Binding StartLineCap}"
                           StrokeLineJoin="{Binding LineJoin}"
                           StrokeMiterLimit="{Binding MiterLimit}"
                           MyIsOffset="{Binding IsOffset}"
                           MyActualSize="{Binding ActualSize, Mode=OneWayToSource}"
                           
                        
                           />


            <Border Background="Black" BorderThickness="4" Opacity="0.2"
                    Width="{Binding Path=ActualWidth}"
                    Height="{Binding Path=ActualHeight}"/>

            <Border Background="YellowGreen" BorderThickness="3" Opacity="0.2"
                    Width="{Binding  MyActualWidth}"
                    Height="{Binding MyActualHeight}"
                    Canvas.Left="0"
                    Canvas.Top="0"/>

            <Border Background="LimeGreen" BorderThickness="2" Opacity="0.2"
                    Width="{Binding MyActualWidth}"
                    Height="{Binding MyActualHeight}"
                    Canvas.Left="{Binding BoundsLeft}"
                    Canvas.Top="{Binding BoundsTop}"/>

            <Border Background="Green" BorderThickness="2" Opacity="0.2"
                    Width="{Binding BoundsWidth}"
                    Height="{Binding BoundsHeight}"
                    Canvas.Left="{Binding BoundsLeft}"
                    Canvas.Top="{Binding BoundsTop}"/>

          </Canvas>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>


  <!--<Style TargetType="{x:Type local:CustomThumb}">
    <Setter Property="MyData" Value="{Binding}"/>
    <Setter Property="Canvas.Left" Value="{Binding X}"/>
    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
    <Setter Property="IsCanDragMove" Value="{Binding IsCanDragMove}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:CustomThumb}">
          <Grid>
            <ContentPresenter x:Name="content" Content="{Binding}"/>
            <Border BorderBrush="Orange" BorderThickness="1"
                    Width="{Binding ElementName=content, Path=ActualWidth}"
                    Height="{Binding ElementName=content, Path=ActualHeight}"/>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>-->



</ResourceDictionary>
